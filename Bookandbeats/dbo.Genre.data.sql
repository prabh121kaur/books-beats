-- Insert 5 rows of data into the Genre table
INSERT INTO [dbo].[Genre] ([Name])
VALUES 
    ('Rock'),        -- GenreId will auto-increment
    ('Pop'),         -- GenreId will auto-increment
    ('Jazz'),        -- GenreId will auto-increment
    ('Classical'),   -- GenreId will auto-increment
    ('Hip-Hop');     -- GenreId will auto-increment
